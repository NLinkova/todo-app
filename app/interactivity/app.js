console.log('Todo app');

function login (evt) {
    evt.preventDefault ();
    method = {method: evt.target.method};
    fetch(evt.target.action, method)
       
}

function login (evt) {
    evt.preventDefault ();
    method = {method: evt.target.method};
    fetch(evt.target.action, method)
       
}